# Enterprise-sim sample app configuration for Kustomize
# Update these values before running `kubectl apply -k sample-app`.
ENVIRONMENT=dev
CLUSTER_NAME=enterprise-sim
NAMESPACE=region-us
REGION=us
APP_NAME=hello-app
ROUTE_HOST=hello
K3S_INGRESS_DOMAIN=localhost
GATEWAY_NAME=dev-sim-gateway
TLS_SECRET_NAME=dev-wildcard-tls
